# AGENTS.md — QQQ 美股市场温度计项目治理索引

## 1. 适用范围与优先级

本文件适用于 `FinRL-Trading` 全仓库，是 Codex 在本项目中工作的总规则和入口索引。

- 用户当前请求、系统规则和安全要求优先于本文件。
- 当前目录及其父目录的 `AGENTS.md` 适用；越靠近被修改文件的规则越具体。
- 子目录中的 `AGENTS.md` 可以补充或收紧本文件，但不能放宽量化审计、数据安全和真实交易边界。
- 每次任务开始时先读取本文件以及目标文件路径上最近的局部 `AGENTS.md`。
- 本文件只治理工作方式，不自动授权修改策略、接入券商、发送订单或部署公网服务。

局部规则：

- 研究与回测：[`research/qqq_drawdown_strategy/AGENTS.md`](research/qqq_drawdown_strategy/AGENTS.md)
- 当前 Streamlit 页面：[`src/web/AGENTS.md`](src/web/AGENTS.md)
- 未来 API 后端：[`src/api/AGENTS.md`](src/api/AGENTS.md)
- 未来 React/Next.js 前端：[`frontend/AGENTS.md`](frontend/AGENTS.md)

项目设计、模块顺序和治理边界见：

- [`docs/QQQ_THERMOMETER_ARCHITECTURE.md`](docs/QQQ_THERMOMETER_ARCHITECTURE.md)
- [`docs/QQQ_THERMOMETER_MODULE_PLAN.md`](docs/QQQ_THERMOMETER_MODULE_PLAN.md)
- [`docs/QQQ_THERMOMETER_GOVERNANCE_BOUNDARIES.md`](docs/QQQ_THERMOMETER_GOVERNANCE_BOUNDARIES.md)
- [`docs/QQQ_VERIFICATION_GOVERNANCE_BASELINE.md`](docs/QQQ_VERIFICATION_GOVERNANCE_BASELINE.md)：独立验证、受保护测试、量化 Gate 与 Evidence 的规范。
- [`docs/PERSONAL_USE_GOVERNANCE.md`](docs/PERSONAL_USE_GOVERNANCE.md)
- [`docs/REPO_CLEANUP_AUDIT_20260812.md`](docs/REPO_CLEANUP_AUDIT_20260812.md)
- [`docs/WORKTREE_RESTORE_PROTOCOL.md`](docs/WORKTREE_RESTORE_PROTOCOL.md)

## 2. 项目目标

这个仓库的产品目标是构建一个供个人使用的“美股市场温度计”与低频纸上组合监控系统：

1. 以冻结、可解释、可复现的策略版本产生状态、目标仓位和信号说明。
2. 用日线收盘数据为主，严格区分“盘中临时观察”和“收盘确认信号”。
3. 展示 QQQ/VOO/SPY 等基准、组合净值、逐年收益、最大回撤、换手、成本和数据质量。
4. 先支持回测与 paper ledger，后续如需真实交易必须另行审批、隔离和验收。
5. 让每一个结论都能追溯到数据源、指标快照、策略版本、信号日期、执行日期和成本假设。

核心原则：温度计是策略状态的解释层，不是第二套未经验证的交易策略；页面不能通过按钮、颜色或文案暗中改变策略参数。

## 2.1 最终个人使用范围

本项目的最终交付目标是“个人本地、低频、纸上组合监控”，不是生产级交易平台。默认范围收窄为：

- 本地或私有网络运行的 Streamlit/轻量页面；
- 日线收盘后刷新，展示确认状态、指标证据、目标权重、纸上净值和数据质量；
- 固定版本的 v10 候选策略、可回放回测和 paper ledger；
- 手动确认、手动暂停和手动回滚；
- 一个主数据源，必要时用第二来源做抽样复核。

以下内容不属于默认个人版，除非用户另行提出并批准独立任务：公网部署、多用户权限、云端数据库、高可用、自动实盘下单、券商密钥托管、期权/融资/保证金、机器学习自动调参和完整 FastAPI/React 产品化拆分。预留目录不代表这些能力必须实现。

个人版的完整边界见 [`docs/PERSONAL_USE_GOVERNANCE.md`](docs/PERSONAL_USE_GOVERNANCE.md)。

## 3. 当前版本与历史版本边界

### 3.1 v10 候选版本

`v10_preserve_shock_recovery` 是当前产品设计中准备作为主要监控来源的候选版本，来源于已完成的恢复阶段实验。它仍然需要在运行时契约、数据质量和 paper shadow 阶段完成后，才可以称为“产品默认版本”。

其研究约束包括：

- 信号在收盘后计算，最早下一个交易日执行；禁止用未来收益或未来收盘价。
- 冲击入口使用 QQQ 5 日跌幅与波动条件的组合：QQQ 5 日收益不高于 -5%，并且满足 VIX 水平或 VIX/VIX3M 期限结构条件。
- 恢复解锁至少满足两个独立确认：QQQ 短期反弹、站上 EMA10、20 日实现波动率下降等；恢复不能只由单一 VIX 变化触发。
- 中期重新开放风险资产还要通过 QQQ 位于 SMA150 之上且 126 日动量为正，并持续满足确认天数。
- 冲击状态、恢复状态有最短持有期和重新冲击滞回，避免单日波动反复切换。

实际阈值、资产权重和版本参数必须来自冻结的配置与报告，不得在前端或接口中复制一份“近似规则”。

### 3.2 历史基线

仓库中已有的 `final_regime_ensemble` 等规则和历史回测输出必须保留，并明确标记为 `legacy` 或 `baseline`。不能因为新页面称其为 v10 就静默替换旧逻辑，也不能覆盖已有报告。

如果当前运行代码仍调用旧基线，系统必须在版本字段中显示真实版本；在 v10 运行时适配完成前，不得把旧基线标成 v10。

## 4. 量化不可妥协项

- 不允许为了提高某一年、某一段或某个单一指标而回看全样本调参。
- 参数、资产集合、执行时点、价格口径、交易成本和数据源都必须有版本号。
- 任何策略修改都生成新版本、新配置、新回测目录和新报告；不得覆盖旧版本。
- 必须同时报告 CAGR、逐年收益、最大回撤、回撤持续时间、Sharpe/Sortino、换手、成本敏感性、与 QQQ 的相关性和不同市场阶段表现。
- 年度比较使用相同交易日、相同起止规则、相同现金假设和相同成本口径；不能选择性省略不利年份。
- 资产收益必须使用与产品定义一致的价格口径，并记录分红、拆分、ETF 上市日期、缺失值和代理数据。
- VIX/VIX3M 是信号，不是可直接持有的资产；VXX、SVXY、QLD、BIL、SGOV、BOXX 的经济含义和风险不能混为一谈。
- 任何“预测”“概率”“将要暴跌”的文案必须改为状态、条件、历史相似阶段或风险提示；不得声称确定预测未来。
- 温度计的 `Signal Agreement` 只能表示信号之间的方向一致程度，不能解释为未来收益概率或胜率。

## 5. 数据、时点与回测规则

默认时点约定：

```text
交易日 t 收盘后：读取截至 t 可获得的数据，计算 signal_date=t 的状态
交易日 t+1：按 next_open / next_close 等冻结执行口径模拟目标仓位
```

必须保存：`as_of`、`signal_date`、`execution_date`、数据源、抓取时间、价格口径、缺失状态、质量状态和策略版本。

数据分层：

1. 原始快照：不可变，保存供应商返回值和抓取元数据。
2. 标准化行情：统一日期、时区、字段、交易日历和调整口径。
3. 指标快照：由标准化行情计算，不能手工编辑。
4. 状态与目标权重：由冻结策略引擎产生，不能由 UI 直接写入。
5. 组合账本：追加写入，修正必须产生更正事件而不是删除历史。

数据质量未通过时，系统应显示 `STALE`、`PARTIAL`、`FAILED` 或 `NEEDS_REVIEW`，而不是悄悄使用旧值继续发布“确认信号”。

## 6. 工程与文件修改规则

- 先读目标文件、调用关系和最近的局部规则，再修改。
- 一个工作单元只处理一个模块；不顺手重构、不批量格式化、不清理用户已有文件。
- 现有脏工作区中的修改属于用户，必须保留；禁止 `git reset --hard`、`git checkout --` 或宽范围删除。
- 文件编辑使用 `apply_patch`；不要用 shell 重定向或脚本写文件来绕过审阅。
- 新增依赖、数据库迁移、外部服务、定时任务和环境变量都要在报告中说明，并在需要时先征得用户同意。
- API 路由只做协议编排；策略、指标和数据质量逻辑放在可测试的服务/领域模块中。
- 前端不复制策略规则，不硬编码权重，不直接拼接数据库查询，不保存密钥。
- 日志不得包含 API key、券商凭证、私人聊天记录、完整账户标识或不必要的个人数据。
- 研究生成物默认写入仓库外的 artifact archive；现有仓库内的历史输出只有在完成引用清单和迁移备份后才能移动或删除。
- 每个任务开始前检查 `git status --porcelain`；任务结束前必须通过 `git diff --check`、局部测试和工作区清洁检查。若开始时已有脏修改，先建立 checkpoint 或停下报告，不把新改动混入旧改动。

## 7. 测试与交付门槛

每个模块完成前至少说明：

- 修改了哪些文件，为什么只修改这些文件；
- 运行了哪些测试、回放或数据质量检查；
- 是否改变了策略结果、接口契约、数据库模式或页面行为；
- 未覆盖的风险、待用户决定事项和回滚方式。

量化模块至少需要确定性单元测试和代表性历史回放；接口模块至少需要契约测试和错误状态测试；前端模块至少需要加载、空数据、过期数据和错误数据状态测试。

没有通过当前模块的验收门槛时，Codex 必须停在该模块，不得自动进入下一模块。

## 8. 禁止事项与停止条件

以下行为必须停止并向用户报告：

- 需要真实券商密钥、下单权限、转账权限或对外发送消息；
- 需要把 paper ledger 改成真实订单执行；
- 需要改变 v10 的策略规则、阈值、资产权重或执行时点，但用户没有明确批准新版本；
- 数据源之间存在无法解释的价格差异、缺失或调整口径冲突；
- 需要删除、覆盖或迁移现有历史报告；
- 发现测试存在未来数据、信号与执行日错位、权重不等于 100% 或重复运行非幂等；
- 用户要求用单一年份、最近行情或视觉评分选择最优参数；
- 需要公开部署但没有身份认证、隐私策略、密钥管理和回滚方案。

## 9. 目标目录索引

```text
research/qqq_drawdown_strategy/  研究、回测、版本化实验，不直接承担产品 API
src/thermometer/                 纯策略状态与温度计领域模型
src/api/                         延期的 FastAPI 协议层；个人版默认不实现
src/storage/                     可选的本地存储适配；不做生产级迁移
src/jobs/                        延期的刷新/发布任务；个人版优先手动运行
src/observability/               延期的运行监控；个人版只保留数据质量与审计结果
src/web/                         当前 Streamlit 原型，保持兼容，不与未来前端混写
frontend/                        延期的 React/Next.js 前端，个人版默认不实现
tests/unit/                      纯函数和领域规则测试
tests/integration/               数据源、存储和服务集成测试
tests/contract/                  API 契约与 schema 测试
tests/replay/                    历史回放、黄金样例和防前视测试
tests/frontend/                  前端组件和页面状态测试
configs/                         可版本控制的非敏感配置
artifacts/                       manifests、reports、schemas 等审计产物
```

目录的详细边界和数据流见架构文档；模块生成顺序见模块计划。当前阶段只建立骨架，不生成业务代码。

## 10. Worktree 清洁与回溯不变量

“干净”不是把文件隐藏起来，而是让源代码、配置和测试都由 Git 记录，让可重建的行情/回测产物有外部清单和 hash，让每次任务都能回到一个明确的 checkpoint。

- 稳定分支只接受经过确认的 checkpoint；实验使用 `codex/<topic>` 分支。
- 一个任务对应一个逻辑提交；不要把多个候选策略、临时输出和治理文档混成一个无法解释的大提交。
- `work/`、`__pycache__/`、临时 CSV/图表和中间回测输出不得作为源码提交；迁移前不得用 `git clean` 删除。
- 任何删除、移动、清理和从 Git 索引移除的操作都必须先有清单、备份位置、hash 和用户确认。
- 任务结束时若不能达到 `git status --porcelain` 为空，必须明确列出剩余脏项和原因，不得宣称完成。

具体操作顺序见 [`docs/WORKTREE_RESTORE_PROTOCOL.md`](docs/WORKTREE_RESTORE_PROTOCOL.md)。
